#general {
    # Основная команда для блокировки (та, что вызывается по сочетанию клавиш)
 #   lock_cmd = caelestia:lock
    # Команда для выполнения перед переходом системы в сон (опционально)
  #  before_sleep_cmd = caelestia:lock
#}

# Пример слушателя, который блокирует экран через 5 минут (300 секунд) бездействия
#listener {
 #   timeout = 900
  #  on-timeout = caelestia:lock  # Команда, выполняемая по таймауту
    # on-resume = ... # Команда при возобновлении активности (необязательно)
#}

# Пример слушателя для отключения дисплея через 10 минут
#listener {
#    timeout = 600
#    on-timeout = hyprctl dispatch dpms off
#    on-resume = hyprctl dispatch dpms on
#}

# Пример слушателя для перевода системы в сон через 30 минут (опционально)
#listener {
 #   timeout = 7200
 #   on-timeout = systemctl suspend
#}
